import React from "react";

const TabStepLabel = ({ children }: { children: React.ReactNode }) => {
  return <div className="text-primary-500 font-medium">{children}</div>;
};

export default TabStepLabel;
